# FTS-Render

## Build

```powershell
git clone git@github.com:CellarCordial/FTS-Render.git
cd FTS-Render
mkdir build
cd build
cmake ..
```

## Control

- Press mouse right button and `w, a, s, d, q, e`